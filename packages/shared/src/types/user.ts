export interface IUser {
  id: string;
  createdAt: string;
  email: string;
  firstName: string;
  lastName: string;
  avatarUrl: string;
  isEmailVerified: boolean;
  lastSignInAt: string;
}
