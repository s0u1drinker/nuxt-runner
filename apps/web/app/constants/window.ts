/** Слушатели событий. */
export const LISTENER = {
  resize: 'resize',
} as const;

/** Задержка для событий. */
export const DELAY = {
  resize: 500,
} as const;

export const POPOVER_ID = {
  userMenu: 'userMenu',
} as const;
